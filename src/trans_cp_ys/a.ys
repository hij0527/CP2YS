(define f::(-> int int)
  (lambda (x::int)
    (let ((y (+ x 1)))
      (= (y 2) true)
      (= (y 3) true)
      (default
        (= (y 4) true)
        (default false)
      ))))
(assert (= (f 1) 2))
(check)
